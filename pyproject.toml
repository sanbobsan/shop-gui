[project]
name = "shop-gui"
version = "0.1.0"
description = ""
authors = [{ name = "sanbob", email = "saintsanbob@gmail.com" }]
requires-python = ">=3.12,<3.15"
dependencies = ["pyside6 (>=6.11.0,<7.0.0)"]

[tool.poetry]
packages = [{ include = "app", from = "src" }]

[dependency-groups]
dev = ["mypy (>=1.20.2,<2.0.0)", "ruff (>=0.15.12,<0.16.0)"]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
